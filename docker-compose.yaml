version: "3"
services:
  peoplesemsegnet-tao-toolkit:
    container_name: peoplesemsegnet-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
